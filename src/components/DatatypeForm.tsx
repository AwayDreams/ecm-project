import { InputAdornment, TextField } from "@mui/material";

export const DataTypeForm = (): JSX.Element => {
    return (
        <TextField
                    id="outlined-password-input"
                    label="Nome"
                    type="text"
                    variant="standard"
                  />
);
}